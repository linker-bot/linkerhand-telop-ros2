# Third-Party Licenses

This repository depends on third-party Python packages including `numpy`, `scipy`,
`PyYAML`, `transforms3d`, `anytree`, `loguru`, `trimesh`, `tqdm`, `colorama`,
and `lxml`.

Each dependency remains governed by its own upstream license.

