[pytest]
testpaths = tests/unit
python_files = test_*.py
markers =
    unit: offline unit tests that do not require ROS graph or hardware
    integration: integration tests that require device, network, or full runtime setup
    hardware: tests that require physical gloves, serial devices, or UDP devices
