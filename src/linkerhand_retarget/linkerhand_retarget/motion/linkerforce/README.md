# LinkerFFG Robot Hand Driver Module

ROS2 driver module for LinkerFFG robot hands.

## Run

```bash
ros2 run linkerhand_retarget handretarget
```

## Notes

- Build from the repository root with `colcon build --symlink-install`.
- This repository is ROS2-only.

