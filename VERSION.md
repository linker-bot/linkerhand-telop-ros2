# 版本说明

## v2.12.6

- 新增 O20 和 O30 机械手型号注册、运行时路由、手型包装器和 URDF 资源。
- 新增 O20 专用 LinkerFFG 映射：O20 张手映射到电机 `0`，握拳映射到 `255`，拇指 opose 输出单独校准，不再直接等同于 URDF 姿态目标。
- 当手套输入匹配 `original`、`opose` 或 `fist` 标定状态时，直接返回对应机械手的精确 URDF 目标，保留标定锚点精度。
- 更新 ROS2 文档和 LinkerFFG 模块文档，补充 O20/O30 支持说明。
- 为 O20/O30 资源、O20 电机方向、O20 拇指 opose 输出以及标定锚点行为增加回归测试覆盖。
- 本次新增：启用握拳采集时，LinkerFFG 自动标定顺序调整为 `fist -> opose -> open`；禁用握拳采集时，顺序调整为 `opose -> open`。
- 补齐 L21 机械手型号枚举、名称映射和 25 自由度长度映射，修复相关集成测试收集失败问题。
- 强化 LinkerFFG 串口连接链路：支持命令行候选串口、配置文件固定串口和 `auto_scan` 自动扫描三种入口，并按 `baudrates` 候选列表尝试连接。
- 自动扫描会过滤 `exclude_ports`，识别 `/dev/ttyUSB*`、`/dev/ttyACM*`、`/dev/ttyXRUSB*` 和 `/dev/ttyOBC*` 设备，并在日志中输出候选串口、波特率和识别结果。
- 串口连接成功后记录手套 `handtype` 和 `version`，运行时根据左右手识别结果初始化对应映射器，避免未连接侧或左右识别异常时误初始化。
- 标定流程支持仅左手或仅右手手套连接的场景；左右均未连接时直接给出失败提示并退出，避免继续访问空串口对象。
- 增加串口帧计数和 O6 右手小指原始数据跳变跟踪日志，便于定位发布话题前的原始数据波动。
- 标定失败或启动失败时会停止串口线程并关闭 MuJoCo 显示，降低进程残留和退出卡死风险。
- 启动 SDK 时会从 `VERSION.md` 读取并打印当前版本号，方便现场确认运行代码版本。

## v2.12.4

- 从混合工作区中拆分为独立 ROS2 仓库。
- 根目录文档简化为仅面向 ROS2 的构建和运行说明。
- 统一所有对外版本号引用为 `2.12.4`。
- 移除 launch 安装说明，改为 `ros2 run`。
- 保留 ROS2 包的子树历史。
