# ROS2 Repository Split Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the extracted ROS2 subtree into a standalone `linkerhand-telop-ros2` repository with root-level `src/`, ROS2-only documentation, and a verified `colcon build --symlink-install` workflow.

**Architecture:** Keep the ROS2 package layout intact under `src/linkerhand_retarget`. Replace mixed ROS1/ROS2 root documentation with ROS2-only README files and release notes. Preserve subtree history in git, but remove outdated launch-install guidance and any ROS1 references from the new repository root.

**Tech Stack:** Git, ROS2 `colcon`, `ament_python`, Python 3, Markdown.

---

### Task 1: Normalize repository root docs

**Files:**
- Create: `README.md`
- Create: `README_zh.md`
- Delete: `Readme.md`
- Delete: `Readme_ZH.md`
- Modify: `startup_linkerforce.sh`
- Create: `VERSION.md`
- Create: `NOTICE`
- Create: `THIRD_PARTY_LICENSES.md`
- Create: `CONTRIBUTING.md`
- Create: `SECURITY.md`
- Create: `CODE_OF_CONDUCT.md`

- [ ] **Step 1: Write the ROS2-only README content**

```md
# LinkerHand ROS2 Teleop SDK

ROS2-only teleoperation and motion retargeting repository.

## Build

```bash
colcon build --symlink-install
source install/setup.bash
```

## Run

```bash
ros2 run linkerhand_retarget handretarget
```
```

- [ ] **Step 2: Make the Chinese README match the same scope**
- [ ] **Step 3: Remove ROS1 launch instructions and keep only `run`-based usage**
- [ ] **Step 4: Add version and license metadata files**

### Task 2: Verify package metadata matches standalone ROS2 repo

**Files:**
- Modify: `src/linkerhand_retarget/package.xml`
- Modify: `src/linkerhand_retarget/setup.py`
- Modify: `src/linkerhand_retarget/setup.cfg`
- Modify: `src/requirements.txt`

- [ ] **Step 1: Confirm package version is `2.12.4` everywhere**
- [ ] **Step 2: Ensure install paths assume a workspace rooted at this repository**
- [ ] **Step 3: Keep only ROS2 runtime dependencies in the root requirements file**

### Task 3: Add repo hygiene defaults

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: Ignore build, install, log, and local report artifacts**

```gitignore
build/
install/
log/
source.txt
```

### Task 4: Validate the standalone repo

**Files:**
- None

- [ ] **Step 1: Run `colcon build --symlink-install` from the repository root**
- [ ] **Step 2: Run a package-level ROS2 executable check**

```bash
ros2 run linkerhand_retarget handretarget --help
```

- [ ] **Step 3: Commit the standalone ROS2 repository state**
